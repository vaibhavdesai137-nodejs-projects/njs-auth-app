
// start mongo
mongod --directoryperdb --dbpath /Users/vaidesai/Development/mongodb/data/db/ --logpath /Users/vaidesai/Development/mongodb/log/mongodb.log --logappend -rest

// run mongo shell
mongo

// create DB
use njs-auth-app-db;
show dbs;

// create collection
db.createCollection('members');
show collections;

// add records
db.members.insert({
    "name": "John Doe",
    "email": "johndoe@gmail.com",
    "username": "johndoe",
    "password": "johndoe"
    
});

db.members.insert({
    "name": "Jane Doe",
    "email": "janedoe@gmail.com",
    "username": "janedoe",
    "password": "janedoe"
});

db.members.find();
db.members.find().pretty();