extends layout

block content
    
    h1 Users
    p This is a NodeJS app that demonstrates the login functionality using passport-local
    br
    p Even though the passwords are hashed/salted, don't be stupid to actually use one of your genuine passwords
    br
    
    p The system currently has following users:
    div.content
        table.table.table-striped
            thead
                tr
                    th Username
                    th Email
                    th Full Name
            tbody
                each user, i in users
                    tr
                        td 
                            #{user.username}
                        td 
                            #{user.email}
                        td 
                            #{user.fullname}